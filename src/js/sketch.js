function setup() {
    let width = window.innerWidth
    let height = window.innerHeight
    let canvas = createCanvas(min(width, height), min(width, height))
    canvas.parent('canvas-container')
    background(0)
}

function draw() {}